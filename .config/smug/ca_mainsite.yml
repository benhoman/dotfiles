session: mainsite

root: ~/Work/consumeraffairs/mainsite/

# before_start:
    # - docker-compose -f docker-compose.yml up -d

# stop:
#     - docker stop $(docker ps -q)

# env:
#     SSHKEY: $(<~/.ssh/id_rsa)
#     KNOWN_HOSTS: $(ssh-keyscan github.com)
#     NPM_TOKEN: $(grep "_authToken" ~/.npmrc | cut -d"=" -f2)

windows:
    - name: code
    - name: ai
    - name: infra
    - name: docker
    - name: lazygit
    #   root: src # relative path to root
